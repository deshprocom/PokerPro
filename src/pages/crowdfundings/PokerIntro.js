import React, {PureComponent} from 'react';
import {
    TouchableOpacity, View, StatusBar,FlatList,
    StyleSheet, Image, Text, Platform
} from 'react-native';
import {Colors, Fonts, Images, ApplicationStyles, Metrics} from '../../Themes';

export default class PokerIntro extends PureComponent {

    render(){
        return(
            <View>
                <Text>PokerIntro</Text>
            </View>
        )
    }
}